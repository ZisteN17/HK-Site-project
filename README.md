# ⚡ Сайт ХК КГАСУ

Официальный сайт хоккейного клуба Казанского государственного архитектурно-строительного университета.

## 🎯 О проекте

**Слоган:** ХК КГАСУ — мы строим будущее

Сайт создан для болельщиков команды, чтобы они могли следить за новостями, составом, расписанием игр и результатами.

## 🚀 Функциональность

- ✅ **Главная страница** — эмблема команды, фото института, слоган и навигация
- ✅ **О команде** — история, дата создания, трофеи
- ✅ **Основной состав** — карточки игроков с фото, позициями и биографиями
- ✅ **Ветераны** — игроки, которые ранее играли за команду
- ✅ **Календарь** — предстоящие игры, результаты матчей, турнирная таблица
- ✅ **Социальные сети** — ссылки на VK, Telegram, Instagram, YouTube
- ✅ **Вступить в команду** — кнопка для связи с менеджером команды

## 📁 Структура проекта

```
HK-Site-project/
├── index.html              # Главная страница
├── pages/                  # Внутренние страницы
│   ├── about.html         # О команде
│   ├── roster.html        # Основной состав
│   ├── veterans.html      # Ветераны
│   ├── calendar.html      # Календарь и результаты
│   └── social.html        # Социальные сети
├── css/
│   └── style.css          # Все стили сайта
├── js/
│   ├── main.js            # Общий JavaScript
│   ├── roster.js          # JS для страницы состава
│   ├── veterans.js        # JS для страницы ветеранов
│   ├── calendar.js        # JS для календаря
│   └── supabase.js        # Интеграция с базой данных
├── images/                # Изображения
│   ├── logo.png           # Эмблема команды
│   ├── institute.jpg      # Фото института
│   ├── players/           # Фото игроков основного состава
│   ├── veterans/          # Фото ветеранов
│   └── opponents/         # Логотипы команд-соперников
└── README.md
```

## 🎨 Цветовая схема

- **Алый красный:** `#DC143C`
- **Черный:** `#000000`
- **Белый:** `#FFFFFF`
- Дополнительные оттенки серого для фонов и текстов

## 🛠️ Технологии

- **HTML5** — структура страниц
- **CSS3** — стилизация и анимации
- **JavaScript (Vanilla JS)** — интерактивность
- **Supabase** — база данных для хранения информации об игроках, матчах и статистике

## 📝 Что нужно сделать дальше

### 1. Добавить контент

#### Изображения:
- [x] `images/logo.jpeg` — эмблема ХК КГАСУ ✅
- [x] `images/institute.jpeg` — фото института КГАСУ ✅
- [ ] `images/players/` — фотографии игроков основного состава
- [ ] `images/veterans/` — фотографии ветеранов команды
- [ ] `images/opponents/` — логотипы команд-соперников

#### Тексты:
- [ ] Обновить текст "О команде" в `pages/about.html`
- [ ] Добавить дату создания команды
- [ ] Добавить достижения и трофеи

### 2. Настроить Supabase

1. **Зарегистрируйтесь на [supabase.com](https://supabase.com)**
2. **Создайте новый проект**
3. **Создайте таблицы:**

#### Таблица `players` (игроки):
```sql
CREATE TABLE players (
    id SERIAL PRIMARY KEY,
    name TEXT NOT NULL,
    number INTEGER NOT NULL,
    position TEXT NOT NULL,
    photo_url TEXT,
    height INTEGER,
    weight INTEGER,
    age INTEGER,
    bio TEXT,
    is_active BOOLEAN DEFAULT true
);
```

#### Таблица `veterans` (ветераны):
```sql
CREATE TABLE veterans (
    id SERIAL PRIMARY KEY,
    name TEXT NOT NULL,
    position TEXT NOT NULL,
    photo_url TEXT,
    years TEXT NOT NULL,
    bio TEXT,
    achievements TEXT
);
```

#### Таблица `games` (матчи):
```sql
CREATE TABLE games (
    id SERIAL PRIMARY KEY,
    season TEXT NOT NULL,
    date TEXT NOT NULL,
    home_team TEXT NOT NULL,
    home_logo TEXT,
    home_score INTEGER,
    away_team TEXT NOT NULL,
    away_logo TEXT,
    away_score INTEGER,
    location TEXT,
    is_played BOOLEAN DEFAULT false
);
```

#### Таблица `standings` (турнирная таблица):
```sql
CREATE TABLE standings (
    id SERIAL PRIMARY KEY,
    season TEXT NOT NULL,
    place INTEGER NOT NULL,
    team TEXT NOT NULL,
    games INTEGER DEFAULT 0,
    wins INTEGER DEFAULT 0,
    losses INTEGER DEFAULT 0,
    goals TEXT DEFAULT '0:0',
    points INTEGER DEFAULT 0
);
```

4. **Получите API ключи:**
   - Перейдите в `Settings` → `API`
   - Скопируйте `URL` и `anon public` ключ
   - Вставьте их в `js/supabase.js`

5. **Подключите Supabase JS библиотеку:**
   Добавьте в `<head>` каждой HTML страницы:
   ```html
   <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
   ```

### 3. Обновить ссылки

В файлах замените placeholder-ссылки на настоящие:

- `index.html` — ссылка на Telegram менеджера (строка 46)
- `social.html` — ссылки на VK, Telegram, Instagram, YouTube (строки 30-90)

### 4. Загрузить на хостинг

Когда сайт будет готов:
1. Загрузите все файлы на ваш хостинг
2. Убедитесь, что структура папок сохранена
3. Проверьте работу всех страниц

## 🖥️ Как запустить локально

1. Откройте файл `index.html` в браузере
2. Для полной работы с Supabase нужен локальный сервер:
   ```bash
   # Если у вас установлен Python
   python -m http.server 8000

   # Или используйте VS Code расширение "Live Server"
   ```
3. Откройте в браузере: `http://localhost:8000`

## 📱 Адаптивность

Сайт полностью адаптирован для:
- 📱 Мобильных устройств (до 768px)
- 💻 Планшетов (768px - 1024px)
- 🖥️ Десктопов (1024px+)

## 🎯 Админ-панель (будущее развитие)

В будущем можно добавить админ-панель для:
- Добавления/редактирования игроков
- Обновления результатов матчей
- Управления календарем игр
- Обновления турнирной таблицы

## 📞 Контакты

- **Команда:** ХК КГАСУ
- **Институт:** Казанский государственный архитектурно-строительный университет

---

**Сделано с ❤️ для ХК КГАСУ**
